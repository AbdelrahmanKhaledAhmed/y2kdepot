[build]
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @workspace/peltsclub run build"

[deploy]
startCommand = "pnpm --filter @workspace/peltsclub run serve"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
